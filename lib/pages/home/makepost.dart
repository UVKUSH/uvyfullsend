import 'package:flutter/material.dart';

class MakePostPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Make Post'),
      ),
      body: Center(
        child: Text('This is the Make Post page'),
      ),
    );
  }
}
