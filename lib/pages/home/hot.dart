import 'package:flutter/material.dart';

class HotPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Hot Content'),
      ),
      body: Center(
        child: Text('Hot Content'),
      ),
    );
  }
}
